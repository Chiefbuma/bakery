import { NextResponse } from 'next/server';

/**
 * NEUTRALIZED: This POS module belongs to the hotel management system.
 * WhiskeDelights Bakery uses a specialized eCommerce checkout flow instead.
 */
export async function POST() {
  return new NextResponse(null, { status: 410 });
}
