export const metadata = {
  title: 'Checkout | WhiskeDelights',
  description: 'Complete your cake booking details.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
